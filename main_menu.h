#pragma once

#include <iostream>
#include <string>

namespace MainMenu
{
	// This will be the main title
	void ShowMainTitle();
	// This will be the options for the user to choose
	void ShowMenuOptions(std::string Input);

}

